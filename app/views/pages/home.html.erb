Home page! Let's transcribe some podcasts!


<!-- form input by Jaimi from chatGPT-->

<h1>Upload Your Podcast for Transcription</h1>

<%= form_with url: podcasts_path, local: true, multipart: true do |form| %>
  <div class="form-group">
    <%= form.label :audio, "Upload Audio File" %>
    <%= form.file_field :audio, accept: 'audio/*', class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.submit "Transcribe", class: "btn btn-primary" %>
  </div>
<% end %>
